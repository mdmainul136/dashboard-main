/** SectionHeader — Label for sidebar navigation groups */
export const SectionHeader = ({ label }: { label: string }) => (
  <p className="mb-2 px-4 text-[10px] font-bold uppercase tracking-[0.15em] text-sidebar-foreground/40">{label}</p>
);
